// Desenvolva uma lógica que leia os valores de A, B e C de uma equação do 
// segundo grau e mostre o valor de Delta. 

let a = 4;
let b = 2;
let c = -6;
const DELTA = (b**2) - 4 * a * (c);

console.log(`com os valores de A=${a}, B=${b} E C=${c} temos o DELTA = ${DELTA}`);